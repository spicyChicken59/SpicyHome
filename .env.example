# Server/CLI/GitHub Actions only. Never put keys in dist/ or browser code.
RENTCAST_API_KEY=
AFDC_API_KEY=
# Scheduled operation also requires GitHub repository variable:
# SPICYHOME_TRACKING_ENABLED=true
# Website publication separately requires a GitHub repository variable:
# SPICYHOME_PAGES_ENABLED=true
# Use a distinct RentCast account usage budget; other callers are outside this app's cap.
