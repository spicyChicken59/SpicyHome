{
  "name": "spicyhome",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dist/app.js && node --check dist/model.js && python -m unittest discover -s tests -p 'test_*.py'",
    "dev": "vite",
    "browser-check": "node tools/browser_check.mjs"
  },
  "devDependencies": {
    "jsdom": "26.1.0",
    "vite": "8.2.2"
  }
}
